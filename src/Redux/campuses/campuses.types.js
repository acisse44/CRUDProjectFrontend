// Define an object to hold the campus action types
const CampusActionType = {
    FETCH_ALL_CAMPUSES: "FETCH_ALL_CAMPUSES",
  };
  
// Export the CampusActionType object as the default export
export default CampusActionType;